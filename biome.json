{
  "$schema": "https://biomejs.dev/schemas/2.5.10/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "formatter": {
    "indentStyle": "space"
  },
  "linter": {
    "enabled": true,
    "rules": {
      "preset": "recommended",
      "complexity": {
        "noExcessiveCognitiveComplexity": "error"
      },
      "style": {
        "useNodejsImportProtocol": "error"
      }
    }
  },
  "overrides": [
    {
      "includes": ["packages/core/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedGlobals": {
              "level": "error",
              "options": {
                "deniedGlobals": {
                  "Buffer": "packages/core stays platform free. See ADR 3.",
                  "__dirname": "packages/core stays platform free. See ADR 3.",
                  "__filename": "packages/core stays platform free. See ADR 3.",
                  "caches": "packages/core stays platform free. See ADR 3.",
                  "document": "packages/core stays platform free. See ADR 3.",
                  "importScripts": "packages/core stays platform free. See ADR 3.",
                  "indexedDB": "packages/core stays platform free. See ADR 3.",
                  "localStorage": "packages/core stays platform free. See ADR 3.",
                  "location": "packages/core stays platform free. See ADR 3.",
                  "navigator": "packages/core stays platform free. See ADR 3.",
                  "process": "packages/core stays platform free. See ADR 3.",
                  "require": "packages/core stays platform free. See ADR 3.",
                  "self": "packages/core stays platform free. See ADR 3.",
                  "sessionStorage": "packages/core stays platform free. See ADR 3.",
                  "window": "packages/core stays platform free. See ADR 3."
                }
              }
            },
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "patterns": [
                  {
                    "group": [
                      "node:*",
                      "node:*/**",
                      "cloudflare:*",
                      "@cloudflare/**",
                      "wrangler",
                      "react*",
                      "react*/**",
                      "@types/react*",
                      "@react-native*/**",
                      "expo*",
                      "expo*/**",
                      "@expo/**",
                      "@seatscout/*"
                    ],
                    "message": "packages/core stays platform free. See ADR 3."
                  }
                ]
              }
            }
          }
        }
      }
    }
  ]
}
