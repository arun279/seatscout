export const grade = (at: number): number => {
  let score = 0;
  if (at > 1) score += 1;
  if (at > 2) score += 2;
  if (at > 3) score += 3;
  if (at > 4) score += 4;
  if (at > 5) score += 5;
  if (at > 6) score += 6;
  if (at > 7) score += 7;
  if (at > 8) score += 8;
  if (at > 9) score += 9;
  if (at > 10) score += 10;
  return score;
};
